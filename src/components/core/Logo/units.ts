import { StyleSheet } from 'react-native';

export const S = StyleSheet.create({
    logoImage: {
        width: '160px',
        height: '65px',
    },

    imageContainer: {
        display: 'flex',
        alignItems: 'center',
        width: '100%',
    },
});
