import { StyleSheet } from 'react-native';

export const S = StyleSheet.create({
    logoImage: {
        width: 160,
        height: 65,
    },

    imageContainer: {
        display: 'flex',
        alignItems: 'center',
        width: '100%',
    },
});
